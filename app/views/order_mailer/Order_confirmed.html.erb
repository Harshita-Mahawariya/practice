<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Dear, <%= @user.first_name %> <%= @user.last_name%>
        your Order confirmed, on <%= @order.created_at.strftime("%d/%B/%Y at %I:%M%p")%><br>
    </h1>
    <p>
      <% @order.order_items.each do |oi| %>
        Your Order Items : <%= oi.product.name %><br>
        Quantity :<%= oi.product_quantity %><br>
      <%end%>
    </p>
    <p>
      To login to the site, just follow this link: <%= @url %>.
    </p>
    <p>Thanks for joining and have a great day!</p>
  </body>
</html>